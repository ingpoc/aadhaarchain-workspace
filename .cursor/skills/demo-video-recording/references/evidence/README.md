# Demo evidence directory

Create one subdirectory per dry-run or recording attempt. Each attempt should contain its step matrix, proof screenshots, and—only after the matrix passes—the final recording reference. Large MP4 files stay ignored or use Git LFS.

An empty directory is not evidence. A demo claim passes only when the owning step matrix links to readable captures for every required beat.
